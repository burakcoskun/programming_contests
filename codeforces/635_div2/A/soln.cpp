#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int a, b, c, d;
int t;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  cin >> t;
  while (t--) {
    cin >> a >> b >> c >> d;
    cout << b << " " << c << " " << c << "\n";
  }
  return 0;
}
