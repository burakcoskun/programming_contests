#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int T;
ll n;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  cin >> T;
  for (int t = 0; t < T; ++t) {
    cin >> n;
    cout << n << "\n";
  }
  return 0;
}
